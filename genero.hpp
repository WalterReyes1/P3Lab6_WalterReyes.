#ifndef ASIENTO_HPP
#define ASIENTO_HPP
#include<iostream>
#include<string>
using namespace std;
class genero {
	private: 
	string nombre;
  public:
  genero();
  genero(string);
  ~genero();
};
#endif
